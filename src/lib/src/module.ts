import { NgModule } from '@angular/core';

/** @deprecated Since version 6, you don't need to import this module anymore. Services are directly injectable. */
@NgModule()
export class LocalStorageModule {}
